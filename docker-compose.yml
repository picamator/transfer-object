services:
  php:
    build:
      context: docker/php
    container_name: transfer-object-php
    extra_hosts:
      - "host.docker.internal:host-gateway"
    expose:
      - 9003
    volumes:
      - ./:/home/transfer/transfer-object
    tty: true
