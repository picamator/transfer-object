services:
  transfer-object-php:
    build:
      context: docker/php
    extra_hosts:
      - "host.docker.internal:host-gateway"
    expose:
      - 9003
    volumes:
      - ./:/home/transfer/transfer-object
