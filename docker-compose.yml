services:
  transfer-object-php:
    build:
      context: docker/php
    container_name: transfer-object-php-8.4
    extra_hosts:
      - "host.docker.internal:host-gateway"
    expose:
      - 9003
    volumes:
      - ./:/home/transfer/transfer-object
    tty: true
