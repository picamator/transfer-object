{
    "$schema": "https://json-schema.org/draft/2020-12",
    "title": "Transfer Object Generator configuration file",
    "description": "Configuration file used for generating Transfer Objects.",
    "type": "object",
    "properties": {
        "generator": {
            "type": "object",
            "properties": {
                "transferNamespace": {
                    "type": "string",
                    "description": "Namespace for the generated Transfer Objects."
                },
                "transferPath": {
                    "type": "string",
                    "description": "Path where the generated Transfer Objects will be stored."
                },
                "definitionPath": {
                    "type": "string",
                    "description": "Path to the Definition Files."
                },
                "hashFileName": {
                    "type": "string",
                    "description": "File name where Transfer Object content hashes are saved for change detection. Optional. Default: transfer-object.list.csv",
                    "pattern": "^[a-zA-Z0-9._-]+\\.csv$"
                }
            },
            "required": ["transferNamespace", "transferPath", "definitionPath"],
            "additionalProperties": false
        }
    },
    "required": ["generator"],
    "additionalProperties": false
}
