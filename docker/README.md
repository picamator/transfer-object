Docker SDK
==========

Docker SDK provides the project's [Development Environment](https://github.com/picamator/transfer-object/wiki/Development-Environment).

All developer operations are managed via the `docker/sdk` script.

Quick Start
-----------

To install the project's development environment, run:

```console
$ docker/sdk install
```

For more commands, run:

```console
$ docker/sdk
```
